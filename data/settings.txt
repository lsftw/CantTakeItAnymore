// Welcome to the settings file!
//
// Format: key = value, keys are case-insensitive, values are case-sensitive
// All text after a "//" is ignored, blank lines are ignored, line order does not matter
//
// If a setting is missing, it will be set to its default value 



startingLevel=TestAirspace
fps=50 // 50 is recommended, most levels assume 50/second, MulisBeam might break at other FPS
show_fps=false
hud_on_top=true

// Background Color, default is r,g,b = 200,255,255 giving a sky blue color
background_red=200
background_blue=255
background_green=255

// It is highly recommended that you keep resolution at 800x600 for intended level gameplay
window_width=800
window_height=600
// Note that the HUD is 100 in height, so the viewport height is 100 less than the screen height

show_tips=true
font=data/font/fp_font.txt
enable_print=true // shows print commands in levels, used for debug info

// if true, ALWAYS uses graphics stored in jar instead of res folder
// note that this prevents easy graphics modification
default_graphics=false

hardmode=false // lose on taking any damage
undying=false // undying prevents lose trigger

debug=false // gives access to debugger ship
spinmode=false // rotated enemy test
